#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() 
{
	printf("Podaj liczbę naturalną n, która będzie końcem przedziału od 1 do n losowanych liczb: ");
	int n;
	scanf("%d", &n);
	while(n<=0)
	{
		printf("Liczba n musi być większa od 0!\n");
		printf("Podaj liczbę: ");
		scanf("%d", &n);
	}

	srand(time(NULL));
	int i;
	for(i=0; i<1; i++)
	{
		n);
	}

	int a;
	printf("Zgadnij liczbę z przedziału od 1 do %d", n);
	while(a<=0)
        {
                printf("Liczba musi być większa od 0!\n");
                printf("Podaj liczbę: ");
                scanf("%d", &a);
        }

	while(a!=n)
	{
		if(a>n)
		{
			printf("Podałeś za dużą liczbę!\n");
		}
		else if(a<n)
		{
			printf("Podałeś za małą liczbę!\n");
		}
	}
	printf("Zgadłeś, wylosowana liczba to %d\n", n);


	return 0;
}
